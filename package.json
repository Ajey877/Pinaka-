{
  "name": "pinaka-agent",
  "version": "0.1.0",
  "private": true,
  "description": "A browser-based coding agent with safe repository inspection, coding, testing, and GitHub workflows.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "node --test packages/core/test/*.test.mjs packages/tools/test/*.test.mjs",
    "lint": "node --check packages/core/src/*.mjs && node --check packages/tools/src/*.mjs && node --check apps/agent/src/*.mjs"
  }
}
